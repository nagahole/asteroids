#include "flatland.h"
#include <stdio.h>

void flatland_protect(void* cluster, void* scanner)
{
    printf("\nSJDNASOIDNOIASNODIest\n");
    // Cluster not clear and no impact
    while ( !asteroid_cluster_clear(cluster) 
         && !asteroid_cluster_impact(cluster))
    {
    
        printf("\nSJDNASOIDNOIASNODIest\n");

    }

    return;
}
